{
  "name": "local-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "./node_modules/.bin/netlify-lambda serve .",
    "build": "./node_modules/.bin/netlify-lambda build ."
  }
}
